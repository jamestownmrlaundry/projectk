/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["source.unsplash.com", "your-image-host.com"], // Add your external image domains here
    },
  };
  
  module.exports = nextConfig;
  